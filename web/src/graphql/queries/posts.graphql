query Posts($limit: Int!, $cursor: DateTime) {
  posts(
      limit: $limit,
      cursor: $cursor
  ) {
    posts {
      ...PostFrag
      textsSnippet
      user {
        id
        userName
        level
      }
    }
    hasMore
  }
}