mutation Register ($inputs: UserRegisterInput!) {
    register (
        inputs: $inputs
    ) {
        errors {
            ...ErrorsFrag
        }
        partialUser {
            ...PartialUserFrag
        }
    }
}